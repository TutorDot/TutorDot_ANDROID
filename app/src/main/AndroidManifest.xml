<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tutor.tutordot">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ScheduleAddActivity"></activity>
        <activity android:name=".ScheduleModificationActivity" />
        <activity android:name=".ClassAddActivity" />
        <activity android:name=".ClassModificationActivity" />
        <activity android:name=".ClassActivity" />
        <activity android:name=".CalenderActivity" />
        <activity android:name=".SignUpActivity" >
        </activity>
        <activity android:name=".StartActivity" />
        <activity android:name="com.tutor.tutordot.LoginActivity" >

        </activity>
        <activity android:name="com.tutor.tutordot.SplashActivity"
            android:theme="@style/SplashTheme">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>